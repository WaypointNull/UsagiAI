# Akumu is its own git repository (https://github.com/WaypointNull/Akumu)
Akumu/

# Tsuki is its own git repository (https://github.com/WaypointNull/Tsuki)
Tsuki/

# Local working notes and data (e.g. data/PLAN.md) — not committed
data/

node_modules/
dist/
build/*
!build/icon.png
release/
out/
*.log

# Store state: downloaded repo copies, installed-plugin metadata and copies
src/registryCache/
src/plugins.json
plugins/*/
